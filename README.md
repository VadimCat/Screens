# Ji2 Screens

Screen navigation primitives used across the project:

- `BaseScreen` for shared screen behavior.
- `Screens` controller for pushing and closing screens.

## Dependencies

This package expects:

- UniTask
- VContainer
- Unity UI
- Odin Inspector (for `SerializedMonoBehaviour`)
- `com.ji2.camera-source`
